import type { Metadata } from "next";
import { Inter, Plus_Jakarta_Sans } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ["latin"],
  variable: "--font-heading",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Alex Rivera | Student & Software Engineer Portfolio",
  description: "Senior Computer Science student at UC Berkeley specializing in Full-Stack Web Development, AI/ML, and Cloud Systems.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${plusJakarta.variable}`}>
      <body className="font-sans antialiased bg-white text-gray-900 selection:bg-brand-100 selection:text-brand-900">
        {children}
      </body>
    </html>
  );
}
